# GeoShift: disable IPv6 stack-wide so traffic cannot bypass Mihomo TUN via IPv6 (see ARCHITECTURE §9).
# Apply: sudo cp sysctl.d/99-geoshift-disable-ipv6.conf /etc/sysctl.d/ && sudo sysctl --system

net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
